{
  "version": 2,
  "buildCommand": "DATABASE_URL=postgresql://postgres:postgres@localhost:5432/mydb npx prisma generate && npx prisma migrate deploy && npm run build",
  "devCommand": "npm run dev",
  "installCommand": "npm install",
  "framework": "nextjs",
  "regions": ["cdg1"]
} 